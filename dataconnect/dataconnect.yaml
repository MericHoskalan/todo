specVersion: "v1beta"
serviceId: "todo-realone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-realone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
